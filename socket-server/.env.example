# Socket.IO Server Environment Configuration
# Copy this to .env and update with your actual database credentials

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=misvord

# Server Configuration
PORT=8010
NODE_ENV=development

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost,http://localhost:80,http://localhost:8080,http://127.0.0.1
